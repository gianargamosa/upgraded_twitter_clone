<% provide(:title, @title) %>
<div class="row-fluid">
  <aside class="col-md-4 profile-card">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user, size: 100 %>
        <%= @user.name %>
      </h1>
    </section>
    <section class='stats'>
      <%= render "shared/stats" %>
    </section>
  </aside>
  <div class="col-md-6 feed-card">
    <% if @user.retweeted.any? %>
      <h3>Retweets (<%= @user.retweeted.count %>)</h3>
      <o1 class="microposts">
        <%= render @retweets %>
      </o1>
      <%= will_paginate @retweets %>
    <% else %>
      <p>No retweets yet, click the thing under someone else's post to favorite it.</p>
    <% end %>
  </div>
</div>